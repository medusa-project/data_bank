= form_with(model: dataset, local: true, :url => "/datasets/#{dataset.key}") do |form|
  - if dataset.errors.any?
    #error_explanation
      %h2
        = pluralize(dataset.errors.count, "error")
        prohibited this dataset from being saved:
      %ul
        - dataset.errors.full_messages.each do |message|
          %li= message

  .field
    = form.label :title
    = form.text_field :title
  .actions
    = form.submit
.fileselect
  %input(type="file" name="selectedFiles" id="selectedFiles" multiple="multiple" )

.datafiles

%div(id="filedata")